import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App placeholder test', (WidgetTester tester) async {
    // Placeholder — will be replaced with real tests per feature.
    expect(1 + 1, 2);
  });
}
